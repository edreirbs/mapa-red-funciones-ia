name: keepalive-supabase
on:
  schedule:
    - cron: '17 12 * * 1,4'  # lunes y jueves 12:17 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping estado del mapa (evita pausa por inactividad del plan gratuito)
        run: |
          curl -sf "https://lapdnydlalrbpdaphfpz.supabase.co/rest/v1/map_state?id=eq.1&select=updated_at" \
            -H "apikey: sb_publishable_iWf-dT48aN8OMGBGS2qBGQ_92LJnopJ"
